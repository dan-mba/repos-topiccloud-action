name: Test

on:
  workflow_dispatch:

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
    - name: Hello world action step
      uses: dan-mba/repos-topiccloud-action@v0.51
      with:
        login: 'dan-mba'
        myToken: ${{secrets.GITHUB_TOKEN}}
   

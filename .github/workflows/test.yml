name: Test

on:
  workflow_dispatch:

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Hello world action step
      uses: dan-mba/repos-topiccloud-action@v0.6
      with:
        github-token: ${{secrets.GITHUB_TOKEN}}
    - name: Find
      run: |
        find . -name cloud.svg
   

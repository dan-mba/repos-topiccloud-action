name: Test

on:
  workflow_dispatch:

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
    - name: Hello world action step
      uses: dan-mba/repos-topiccloud-action@0.54
      with:
        github-token: ${{secrets.GITHUB_TOKEN}}
   

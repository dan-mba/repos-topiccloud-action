name: Test

on:
  workflow_dispatch:

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Generate Word Cloud
      uses: dan-mba/repos-topiccloud-action@master
      with:
        github-token: ${{secrets.GITHUB_TOKEN}}
        test-login: torch2424
    - name: Find
      run: |
        cat cloud.svg
   
